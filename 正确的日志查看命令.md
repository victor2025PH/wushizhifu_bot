# 正确的日志查看命令

## ⚠️ 重要：服务名称

**正确的服务名称是：`wushizhifu-bot`**（不是 `wushipay-bot`）

## 📋 正确的日志查看命令

### 1. 查看实时日志（跟随最新日志）

```bash
sudo journalctl -u wushizhifu-bot.service -f
```

### 2. 查看最近 50 条日志

```bash
sudo journalctl -u wushizhifu-bot.service -n 50 --no-pager
```

### 3. 查看最近 100 条日志（包含错误）

```bash
sudo journalctl -u wushizhifu-bot.service -n 100 --no-pager | grep -E "(ERROR|Error|Exception|Traceback|Menu button|Bot description|Initialized)"
```

### 4. 查看今天的日志

```bash
sudo journalctl -u wushizhifu-bot.service --since today --no-pager
```

### 5. 查看最近 5 分钟的错误日志

```bash
sudo journalctl -u wushizhifu-bot.service --since "5 minutes ago" --no-pager | grep -i error
```

## 🔍 检查服务状态

### 检查服务是否运行

```bash
sudo systemctl status wushizhifu-bot.service
```

### 检查服务是否启用

```bash
sudo systemctl is-enabled wushizhifu-bot.service
```

### 检查服务进程

```bash
ps aux | grep "wushizhifu/bot/bot.py" | grep -v grep
```

## 🐛 如果看到错误

### 查看完整错误信息

```bash
sudo journalctl -u wushizhifu-bot.service -n 200 --no-pager | tail -50
```

### 查看 Python 错误堆栈

```bash
sudo journalctl -u wushizhifu-bot.service -n 200 --no-pager | grep -A 20 "Traceback"
```

## 📝 常用组合命令

### 一键查看状态和日志

```bash
echo "=== 服务状态 ===" && \
sudo systemctl status wushizhifu-bot.service --no-pager | head -15 && \
echo "" && \
echo "=== 最近日志（关键信息）===" && \
sudo journalctl -u wushizhifu-bot.service -n 30 --no-pager | grep -E "(Menu button|Bot description|Initialized|ERROR|Error|Exception|Traceback)" | tail -20
```

### 查看启动日志

```bash
sudo journalctl -u wushizhifu-bot.service --since "10 minutes ago" --no-pager | grep -E "(Starting|Started|Menu button|Bot description|Initialized|ERROR)"
```

